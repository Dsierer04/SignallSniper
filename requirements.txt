# Core -- the trading path. Deliberately small.
httpx>=0.27
websockets>=12.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# Legacy Reddit scraper (legacy/reddit_sentiment.py) -- NOT in the trading path.
# Install only if you want the pre-market thesis screen. `transformers` pulls in
# torch, which is ~2GB and has no business near a latency-sensitive process.
#   praw
#   transformers
